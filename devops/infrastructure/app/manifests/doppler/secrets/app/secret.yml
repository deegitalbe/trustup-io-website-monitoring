apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: trustup-io-app-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: trustup-io-app-secret
  managedSecret:
    name: trustup-io-app-secret
    namespace: app